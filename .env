## Shared dev configuration for docker-compose / services
SPRING_PROFILES_ACTIVE=dev

# Ports
CORE_PORT=8080
PRODUCER_PORT=8082
UI_PORT=3000

# Service discovery inside compose
CORE_SERVICE_URL=http://core-service:${CORE_PORT}
PRODUCER_SERVICE_URL=http://producer-service:${PRODUCER_PORT}

# UI build-time env (blank => use Vite proxy + relative paths)
VITE_CORE_URL=
